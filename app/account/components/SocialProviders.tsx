"use client"

// OAuth providers temporarily disabled - using email/password authentication only
// Google OAuth can be re-enabled later if needed

export function SocialProviders() {
  return null
}
